<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE rfc [
  <!ENTITY RFC2119 SYSTEM "https://bib.ietf.org/public/rfc/bibxml/reference.RFC.2119.xml">
  <!ENTITY RFC8174 SYSTEM "https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8174.xml">
]>
<rfc xmlns:xi="http://www.w3.org/2001/XInclude"
     docName="draft-tcr-tef-00"
     category="info"
     ipr="trust200902"
     submissionType="independent">

  <front>
    <title abbrev="TEF">Triageable Evidence Format (TEF)</title>
    <author initials="J." surname="Carroll" fullname="John Carroll">
      <organization>The Clearing Room</organization>
      <address>
        <email>jc@thecontractor.io</email>
      </address>
    </author>
    <date year="2026" month="April" day="9"/>
    <area>Security</area>

    <abstract>
      <t>This document defines the Triageable Evidence Format (TEF), a YAML-based format for structured vulnerability evidence submission. TEF provides a standard way for security researchers to describe software defects so that triage systems, whether human or automated, can classify them without ambiguity.</t>
      <t>TEF combines YAML data structure with a Given/When/Then evidence model. Each submission carries structured metadata, one or more evidence scenarios with preconditions, triggers, and observed outcomes, location data identifying where the defect exists, and reproduction artefacts proving its presence.</t>
      <t>TEF is an intake format. It does not replace vulnerability publication formats (CVE, CSAF, OSV) or scan output formats (SARIF). It fills the gap between discovery and classification.</t>
    </abstract>
  </front>

  <middle>
    <section anchor="introduction" numbered="true" toc="default">
      <name>Introduction</name>
      <t>Vulnerability disclosure relies on structured formats at the publication stage (CVE records, CSAF advisories, OSV entries) and at the scanning stage (SARIF). There is no equivalent standard for the intake stage: what a researcher submits when reporting a software defect to a coordination body, vendor, or disclosure platform.</t>
      <t>Most intake systems accept free text. This creates ambiguity, inconsistency, and friction. A researcher describes their finding in prose. A triager interprets the prose, potentially misunderstanding preconditions, confusing the trigger with the outcome, or missing evidence entirely. At scale, this process does not work.</t>
      <t>TEF addresses this gap by defining a structured, machine-parseable, human-readable format for vulnerability evidence.</t>

      <section anchor="requirements-language" numbered="true" toc="default">
        <name>Requirements Language</name>
        <t>The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT", "SHOULD", "SHOULD NOT", "RECOMMENDED", "NOT RECOMMENDED", "MAY", and "OPTIONAL" in this document are to be interpreted as described in BCP 14 <xref target="RFC2119"/> <xref target="RFC8174"/> when, and only when, they appear in all capitals, as shown here.</t>
      </section>
    </section>

    <section anchor="format-overview" numbered="true" toc="default">
      <name>Format Overview</name>
      <t>A TEF document is a YAML file containing two required top-level keys: "defect" and "evidence".</t>
      <t>The "defect" key contains structured metadata about the affected product, vendor, target type, weakness classification, affected versions, and defect manifestation pattern.</t>
      <t>The "evidence" key contains one or more scenarios, each describing a distinct piece of evidence using a Given/When/Then structure, an optional location block, and an optional reproduction block.</t>
    </section>

    <section anchor="schema" numbered="true" toc="default">
      <name>Schema</name>

      <section anchor="defect-block" numbered="true" toc="default">
        <name>Defect Block (REQUIRED)</name>
        <t>The "defect" block MUST contain the following fields:</t>
        <ul>
          <li>"product" (string, REQUIRED): The name of the affected product.</li>
          <li>"vendor" (string, REQUIRED): The vendor domain or identifier.</li>
          <li>"type" (string, REQUIRED): The target type. See <xref target="target-types"/>.</li>
          <li>"cwe" (integer, REQUIRED): The CWE identifier for the weakness class.</li>
          <li>"versions" (array of strings, REQUIRED): The affected version range.</li>
          <li>"presence" (string, REQUIRED): The defect manifestation pattern. See <xref target="presence-patterns"/>.</li>
        </ul>
        <t>The "defect" block MAY contain:</t>
        <ul>
          <li>"os" (string): The operating system or platform.</li>
          <li>"description" (string): Freeform context.</li>
          <li>"cvss" (string): CVSS vector or score.</li>
          <li>"references" (array of strings): URLs, CVE IDs, advisory links.</li>
          <li>"reproduction_rate" (string): For probabilistic or timing-dependent defects (e.g., "15/100").</li>
          <li>"environment" (string): Specific environment if relevant.</li>
        </ul>
      </section>

      <section anchor="evidence-block" numbered="true" toc="default">
        <name>Evidence Block (REQUIRED)</name>
        <t>The "evidence" block MUST contain one or more scenario entries. Each scenario MUST contain:</t>
        <ul>
          <li>"title" (string, REQUIRED): A short name for the scenario.</li>
          <li>"given" (array of strings, REQUIRED): Preconditions that must be true before the trigger.</li>
          <li>"when" (array of strings, REQUIRED): The action or input that reveals or triggers the defect.</li>
          <li>"then" (array of strings, REQUIRED): The observed outcome that proves the defect exists.</li>
        </ul>
        <t>Each scenario MAY contain:</t>
        <ul>
          <li>"location": A block identifying where the defect exists. See <xref target="location-block"/>.</li>
          <li>"reproduction": A block carrying proof artefacts. See <xref target="reproduction-block"/>.</li>
        </ul>
      </section>
    </section>

    <section anchor="target-types" numbered="true" toc="default">
      <name>Target Types</name>
      <t>The "type" field in the "defect" block MUST be one of the following values: "web", "api", "mobile_android", "mobile_ios", "mobile", "desktop", "server", "os", "embedded", "network", "cloud", "ot", "ai_ml", "supply_chain".</t>
      <t>Producers SHOULD use "mobile_android" or "mobile_ios" rather than "mobile" when the defect is platform-specific. The "desktop" type refers to the application, not the operating system. The "server" type covers software running as a service. Web applications running on server software are "web".</t>
    </section>

    <section anchor="presence-patterns" numbered="true" toc="default">
      <name>Presence Patterns</name>
      <t>The "presence" field MUST be one of: "static_artefact", "static_config", "behavioural_deterministic", "behavioural_stateful", "behavioural_timing", "probabilistic", "environmental", "design_flaw", "supply_chain".</t>

      <t>"static_artefact": The defect exists in the artefact itself and can be confirmed without execution.</t>
      <t>"static_config": Observable in configuration, headers, DNS, or policy without exploitation.</t>
      <t>"behavioural_deterministic": Requires execution but same input always produces same result.</t>
      <t>"behavioural_stateful": Requires specific application state or multi-step interaction.</t>
      <t>"behavioural_timing": Depends on timing or concurrency. Submissions SHOULD include "reproduction_rate".</t>
      <t>"probabilistic": Same input may produce different outcomes because the system is non-deterministic. Distinct from timing-dependent. Submissions SHOULD include "reproduction_rate".</t>
      <t>"environmental": Only manifests in specific environments. Submissions SHOULD include comparison.</t>
      <t>"design_flaw": The design itself is insecure. No single code fix resolves it.</t>
      <t>"supply_chain": Inherited from a third-party component or dependency.</t>
    </section>

    <section anchor="location-block" numbered="true" toc="default">
      <name>Location Block</name>
      <t>The "location" block identifies where the defect exists. It MAY appear on any evidence scenario.</t>
      <ul>
        <li>"type" (string, REQUIRED): One of "endpoint", "file", "function", "binary_offset", "register", "port", "config", "path".</li>
        <li>"value" (string, REQUIRED): The specific location.</li>
        <li>"context" (string, OPTIONAL): Broader context.</li>
      </ul>
    </section>

    <section anchor="reproduction-block" numbered="true" toc="default">
      <name>Reproduction Block</name>
      <t>The "reproduction" block carries raw proof artefacts. All fields are OPTIONAL.</t>
      <ul>
        <li>"commands" (array of strings): Exact commands executed.</li>
        <li>"code" (string): Vulnerable code snippet or exploit script.</li>
        <li>"request" (string): Raw protocol request.</li>
        <li>"response" (string): Raw protocol response.</li>
        <li>"logs" (string): Relevant log entries.</li>
        <li>"output" (string): Tool output, crash dumps, extraction results.</li>
        <li>"files" (array of objects): References to attached evidence files, each with "name" (string), "hash" (string, SHA-256), and "type" (string).</li>
      </ul>
    </section>

    <section anchor="defect-classes" numbered="true" toc="default">
      <name>Defect Classes</name>
      <t>TEF recognises 19 defect classes across two categories. The defect class is not required but is RECOMMENDED. Producers MAY use the "bugClass" field.</t>
      <t>Code defects: injection, auth, crypto, data_exposure, access_control, deserialization, file_handling, ssrf, memory, logic, supply_chain, ai_ml.</t>
      <t>Configuration defects: config_tls, config_headers, config_secrets, config_permissions, config_logging, config_dns, config_default.</t>
      <t>The "ai_ml" class covers defects in AI and machine learning systems including prompt injection, training data poisoning, model extraction, adversarial inputs, excessive agent permissions, RAG corpus manipulation, and model supply chain compromise.</t>
    </section>

    <section anchor="conformance" numbered="true" toc="default">
      <name>Conformance</name>
      <t>A TEF producer MUST include all REQUIRED fields. A TEF consumer MUST accept documents with unknown fields and MUST NOT reject them.</t>
      <t>A TEF document is valid if: (1) it is valid YAML, (2) the "defect" block contains all REQUIRED fields, (3) the "evidence" block contains at least one scenario, (4) each scenario contains "title", "given", "when", and "then", (5) the "type" value is from <xref target="target-types"/>, (6) the "presence" value is from <xref target="presence-patterns"/>.</t>
    </section>

    <section anchor="relationship" numbered="true" toc="default">
      <name>Relationship to Other Formats</name>
      <t>TEF is an intake format. It captures what a researcher observed, structured for triage. After triage, findings are published as CVE records, CSAF <xref target="CSAF"/> advisories, or correlated with SARIF <xref target="SARIF"/> output. The Common Weakness Enumeration <xref target="CWE"/> provides the defect taxonomy. TEF does not duplicate these formats. It precedes them in the vulnerability lifecycle.</t>
    </section>

    <section anchor="security-considerations" numbered="true" toc="default">
      <name>Security Considerations</name>
      <t>TEF documents may contain offensive security content: exploit code, injection payloads, shell commands, memory corruption details, and proof-of-concept material. Systems processing TEF documents MUST treat all user-supplied content as untrusted input.</t>
      <t>TEF documents SHOULD NOT contain plaintext credentials or unredacted sensitive data belonging to third parties. Producers SHOULD redact sensitive values while preserving enough information for verification.</t>
      <t>The "reproduction" block may reference attached files by SHA-256 hash. Consumers MUST NOT execute attached files without explicit user authorisation.</t>
    </section>

    <section anchor="iana-considerations" numbered="true" toc="default">
      <name>IANA Considerations</name>
      <t>This document has no IANA actions. A media type registration for "application/tef+yaml" may be requested in a future version.</t>
    </section>
  </middle>

  <back>
    <references>
      <name>References</name>
      <references>
        <name>Normative References</name>
        &RFC2119;
        &RFC8174;
      </references>
      <references>
        <name>Informative References</name>
        <reference anchor="CWE" target="https://cwe.mitre.org/">
          <front><title>Common Weakness Enumeration</title><author><organization>MITRE</organization></author><date/></front>
        </reference>
        <reference anchor="SARIF" target="https://docs.oasis-open.org/sarif/sarif/v2.1.0/sarif-v2.1.0.html">
          <front><title>Static Analysis Results Interchange Format (SARIF) Version 2.1.0</title><author><organization>OASIS</organization></author><date year="2020"/></front>
        </reference>
        <reference anchor="CSAF" target="https://docs.oasis-open.org/csaf/csaf/v2.0/csaf-v2.0.html">
          <front><title>Common Security Advisory Framework (CSAF) Version 2.0</title><author><organization>OASIS</organization></author><date year="2022"/></front>
        </reference>
      </references>
    </references>

    <section anchor="acknowledgments" numbered="false" toc="default">
      <name>Acknowledgments</name>
      <t>The TEF format was developed through implementation experience at The Clearing Room, an independent vulnerability disclosure clearing house. Feedback from the security research community, particularly on presence pattern classification and AI/ML defect coverage, shaped the current specification.</t>
    </section>

    <section anchor="examples" numbered="false" toc="default">
      <name>Examples</name>
      <t>Complete TEF examples covering all target types and presence patterns are maintained at the TEF template library: https://theclearingroom.io/templates</t>
    </section>
  </back>
</rfc>
